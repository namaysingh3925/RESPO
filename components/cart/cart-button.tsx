"use client";

// STUB — replaced by the ordering build step. Export name and props are frozen.

export function CartButton({ className }: { className?: string }) {
  return <button type="button" className={className} aria-label="Open cart" />;
}
